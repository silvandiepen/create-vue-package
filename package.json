{
  "name": "create-vue-package",
  "version": "0.0.0",
  "description": "Vue Component and Directive Packager",
  "main": "dist/create-vue-package.cjs.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test:cov": "npm run lint && jest --coverage",
    "test": "npm run lint && jest",
    "lint": "standard",
    "prepublishOnly": "npm run build",
    "build": "bili",
    "try": "rm -rf test && node bin/cli.js test"
  },
  "repository": {
    "url": "silvandiepen/create-vue-package",
    "type": "git"
  },
  "author": "silvandiepen<me@silvandiepen.com>",
  "license": "MIT",
  "dependencies": {
    "sao": "^1.6.1"
  },
  "devDependencies": {
    "prettier": "^1.15.2",
    "standard": "^12.0.0",
    "jest": "^23.6.0",
    "lint-staged": "^7.2.0",
    "husky": "^1.0.0-rc.13",
    "bili": "^3.3.0"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js, !template/**": [
      "standard --fix",
      "git add"
    ],
    "*.{json,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
